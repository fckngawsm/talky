{
  "private": true,
  "workspaces": [
    "packages/core/*"
  ],
  "devDependencies": {
    "lerna": "^8.2.2"
  },
  "scripts": {
    "bootstrap": "yarn install && yarn init",
    "init": "node init.js && lerna exec -- yarn install",
    "reset": "lerna clean -y && rm -rf node_modules && yarn install",
    "dev:client": "lerna run dev  --scope=frontend"
  },
  "name": "video",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/fckngawsm/talky.git",
  "author": "Kirill",
  "license": "MIT"
}
