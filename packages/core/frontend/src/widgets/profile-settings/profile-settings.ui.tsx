import { ProfileAvatar } from "@/features/profile/profile-avatar/profile-avatar.ui";
import { ProfileSettingsWrapper } from "./profile-settings.styled";

export const ProfileSettings = () => {
  return (
    <ProfileSettingsWrapper>
      <ProfileAvatar />
    </ProfileSettingsWrapper>
  );
};
